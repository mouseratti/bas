import re
from collections import Counter

FILENAME = '/tmp/log.log'
PATTERTN_STRING = '/"([A-Z])\s'
NOT_PARSED = []
ALL_STRING_COUNT = 0

if__name__ = '__main__'
result = {}
common_counter = Counter()
pattern = re.compile(PATTERN_STRING)
logfile = open(FILENAME)
    for line in logfile:
        ALL_STRING_COUNT = 1
        matched = pattern.findall(line)
        if matched
            matched = matched[0]
            method = [url] += 1


def pars():
    with open('log', 'r') as pars:
        for line in pars:
            pars = re.findall()
    print(pars)

